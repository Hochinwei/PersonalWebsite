<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Ho Chin Wei</title>
<link>https://hochinwei.com/blog/</link>
<atom:link href="https://hochinwei.com/blog/index.xml" rel="self" type="application/rss+xml"/>
<description>PhD researcher exploring AI&#39;s impact on human cognition and behavior</description>
<generator>quarto-1.9.37</generator>
<lastBuildDate>Fri, 21 Aug 2026 09:31:19 GMT</lastBuildDate>
<item>
  <title>Example Post: Blogging Template</title>
  <dc:creator>Ho Chin Wei</dc:creator>
  <link>https://hochinwei.com/blog/example-post/</link>
  <description><![CDATA[ 





<!--
  NOTE TO FUTURE ME: don't write new posts from a blank file. Copy this
  whole "example-post" folder, rename it (the folder name becomes part
  of the post's URL, so use something short and readable, e.g.
  "phd-year-one-reflections"), edit index.qmd inside it, and delete the
  "draft: true" line above once it's ready to publish. This comment and
  the rest of this page are template scaffolding - they won't appear on
  the live site because of "draft: true", and this HTML comment itself
  never renders at all.
-->
<p>This page is a working template, not a real post - it exists to show the correct pattern for writing one. Everything below demonstrates something worth copying.</p>
<section id="heading-structure" class="level2">
<h2 class="anchored" data-anchor-id="heading-structure">Heading Structure</h2>
<p>Quarto turns the <code>title:</code> field above into the page’s main heading automatically, so headings inside the post body should start one level down, at <code>##</code> (not <code>#</code> - using <code>#</code> here would create a second, duplicate top-level heading).</p>
<ul>
<li><code>##</code> (H2) marks a major section of the post, like this one.</li>
<li><code>###</code> (H3) marks a subsection within it, like the one directly below.</li>
</ul>
<p>Quarto builds the page’s table of contents from these headings, so keeping them in order (H2 before H3, no skipped levels) keeps the TOC sensible.</p>
<section id="a-subsection-example" class="level3">
<h3 class="anchored" data-anchor-id="a-subsection-example">A Subsection Example</h3>
<p>This is what a subsection looks like - one level below the section it belongs to.</p>
</section>
</section>
<section id="code-chunks" class="level2">
<h2 class="anchored" data-anchor-id="code-chunks">Code Chunks</h2>
<p>Code chunks use three backticks and curly braces naming the language, e.g.&nbsp;<code>```{r}</code> or <code>```{python}</code>. The <code>#|</code> lines at the top of a chunk are <strong>chunk options</strong> - settings for that one chunk only. Two of the most useful:</p>
<ul>
<li><code>echo</code> - whether the code itself is shown on the page (<code>true</code>) or hidden, showing only its output (<code>false</code>).</li>
<li><code>eval</code> - whether the code actually <em>runs</em> when the site is rendered (<code>true</code>), or is just displayed as text without being executed (<code>false</code>). Useful for showing code that depends on something not guaranteed to be available at render time - a package, an API key, a large dataset - without breaking the build.</li>
</ul>
<section id="r-example" class="level3">
<h3 class="anchored" data-anchor-id="r-example">R example</h3>
<p>This site’s other R pages (the LinkedIn games and Bayesian analysis projects) both use this same R engine, so this is the more “native” language for this site.</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">mean</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">15</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">16</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">23</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">42</span>))</span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code>[1] 18</code></pre>
</div>
</div>
</section>
<section id="python-example" class="level3">
<h3 class="anchored" data-anchor-id="python-example">Python example</h3>
<p>Python chunks work the same way, through the same R-based engine (via a package called <code>reticulate</code> that lets R call out to Python) - as long as the post’s front matter does <em>not</em> add a <code>jupyter: python3</code> line. Setting <code>eval: false</code> here on purpose, just to demonstrate the option; remove it in a real post if you want the code to actually run.</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> statistics</span>
<span id="cb3-2">statistics.mean([<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">15</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">16</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">23</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">42</span>])</span></code></pre></div></div>
</div>


</section>
</section>

 ]]></description>
  <category>tutorial</category>
  <category>template</category>
  <guid>https://hochinwei.com/blog/example-post/</guid>
  <pubDate>Fri, 21 Aug 2026 09:31:19 GMT</pubDate>
</item>
<item>
  <title>What we lose when we stop being able to tell</title>
  <dc:creator>Ho Chin Wei</dc:creator>
  <link>https://hochinwei.com/blog/template-post/</link>
  <description><![CDATA[ 





<p>Open with the claim, not the background. One short paragraph that tells the reader what the post argues, so they can decide in ten seconds whether to keep going. Assume they have not read anything else on this site.</p>
<p>Then the second paragraph does the setup — why the question matters, what the usual framing gets wrong. Prose runs at a 68-character measure and is justified, so paragraphs of three to six sentences sit better than very long or very short ones.</p>
<section id="the-usual-framing" class="level2">
<h2 class="anchored" data-anchor-id="the-usual-framing">The usual framing</h2>
<p>Use <code>##</code> for sections. Each one gets a hairline rule above it, which is what gives a long post its rhythm — so use them generously, roughly every three or four paragraphs, and give them real titles rather than “Background” or “Discussion”.</p>
<p>Inline formatting works as you’d expect: <em>emphasis</em>, <strong>strong</strong>, <code>inline code</code>, and <a href="https://quarto.org">a link</a>. A footnote goes like this<sup>1</sup> and collects at the bottom of the post.</p>
<blockquote class="blockquote">
<p>A pull quote is a hairline on the left and larger type. Use one when a sentence is worth stopping on — not to break up text you think is boring.</p>
</blockquote>
<section id="a-sub-point" class="level3">
<h3 class="anchored" data-anchor-id="a-sub-point">A sub-point</h3>
<p><code>###</code> is available for a level below, though most posts don’t need it.</p>
</section>
</section>
<section id="something-worth-flagging" class="level2">
<h2 class="anchored" data-anchor-id="something-worth-flagging">Something worth flagging</h2>
<div class="callout callout-style-default callout-note callout-titled">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Note</span>A note to one side
</div>
</div>
<div class="callout-body-container callout-body">
<p>Callouts render as a hairline box with an accent rule on the left. <code>.callout-note</code> is the neutral one; <code>.callout-warning</code>, <code>.callout-important</code> and <code>.callout-tip</code> all work and look the same in this theme. Good for a methodological caveat or an aside that would derail the paragraph.</p>
</div>
</div>
</section>
<section id="showing-your-work" class="level2">
<h2 class="anchored" data-anchor-id="showing-your-work">Showing your work</h2>
<p>If the post needs numbers, write the chunk and let it run — the output is framed automatically and the code block keeps its highlighting:</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Any R works here. Keep chunks short enough to read on screen.</span></span>
<span id="cb1-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">summary</span>(cars<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">$</span>speed)</span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code>   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. 
    4.0    12.0    15.0    15.4    19.0    25.0 </code></pre>
</div>
</div>
<p>Plots get a caption and are matted like a photograph. <code>fig-cap</code> writes the caption; <code>fig-alt</code> is the screen-reader description and is worth filling in:</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plot</span>(cars<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">$</span>speed, cars<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">$</span>dist,</span>
<span id="cb3-2">     <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">pch =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">16</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">col =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"#4b7a55"</span>,</span>
<span id="cb3-3">     <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">xlab =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Speed (mph)"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ylab =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Stopping distance (ft)"</span>)</span></code></pre></div></div>
<div class="cell-output-display">
<div id="fig-demo" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Scatterplot showing stopping distance increasing with speed.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-demo-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hochinwei.com/blog/template-post/index_files/figure-html/fig-demo-1.png" class="img-fluid figure-img" alt="Scatterplot showing stopping distance increasing with speed." width="672">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-demo-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Figure 1. Stopping distance against speed, from the built-in <code>cars</code> data.
</figcaption>
</figure>
</div>
</div>
</div>
<p>Refer to it in the text as Figure&nbsp;1 and Quarto numbers and links it for you.</p>
<p>An ordinary image, if you want one without R:</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://hochinwei.com/assets/profile-main.jpg" class="img-fluid figure-img" alt="Placeholder image." width="480"></p>
<figcaption>A caption sits under the plate, left-aligned.</figcaption>
</figure>
</div>
</section>
<section id="closing" class="level2">
<h2 class="anchored" data-anchor-id="closing">Closing</h2>
<p>Land the argument. One paragraph, no summary of what you just said — say the thing you were building toward, or the question you’re leaving open.</p>
<hr>
</section>
<section id="how-to-use-this-file" class="level2">
<h2 class="anchored" data-anchor-id="how-to-use-this-file">How to use this file</h2>
<p>Copy the whole <code>template-post/</code> folder, rename it to a slug (<code>blog/detection-is-the-wrong-test/</code>), replace the front matter and the body, and delete this last section. The folder name becomes the URL. Keep each post’s images inside its own folder so a post stays self-contained.</p>


</section>


<div id="quarto-appendix" class="default"><section id="footnotes" class="footnotes footnotes-end-of-document"><h2 class="anchored quarto-appendix-heading">Footnotes</h2>

<ol>
<li id="fn1"><p>Footnotes are good for the caveat you don’t want in the main sentence.↩︎</p></li>
</ol>
</section></div> ]]></description>
  <category>authenticity</category>
  <category>AI</category>
  <category>cognition</category>
  <guid>https://hochinwei.com/blog/template-post/</guid>
  <pubDate>Mon, 10 Aug 2026 23:00:00 GMT</pubDate>
</item>
</channel>
</rss>
