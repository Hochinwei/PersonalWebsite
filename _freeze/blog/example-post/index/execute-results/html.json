{
  "hash": "cd0b3dadfdaed662214cc8873ad85756",
  "result": {
    "engine": "knitr",
    "markdown": "---\n# The page title. Shown at the top of the post, in browser tabs, and as\n# the headline in the blog listing page (blog.qmd).\ntitle: \"Example Post: Blogging Template\"\n\n# Your name as the post's author. Shown under the title.\nauthor: \"Ho Chin Wei\"\n\n# The publication date. \"last-modified\" (WITH the hyphen) always shows\n# today's render date - useful for pages you expect to keep updating.\n# For a normal post you'll usually want a fixed date instead, e.g.\n# date: \"2026-08-05\" - so the post keeps the date you actually wrote it,\n# even if you re-render the site later.\ndate: last-modified\n\n# One or more tags. Because blog.qmd's listing has \"categories: true\",\n# these become clickable filter buttons on the blog listing page.\ncategories: [tutorial, template]\n\n# A short one- or two-sentence summary. Shown under the title on this\n# page, and used as the preview text in the blog listing and RSS feed.\ndescription: \"A commented template showing the correct front matter, heading structure, and code chunk options for writing a new blog post.\"\n\n# Optional: path to an image used as this post's thumbnail in the\n# listing (e.g. image: thumbnail.jpg, a file placed in this same\n# folder). If you leave it out, Quarto automatically uses the first\n# image it finds in the post's own content instead.\n# image: thumbnail.jpg\n\n# Keeps this template out of the public blog listing and RSS feed -\n# Quarto skips anything marked as a draft. Delete this line when you\n# copy this folder to start a real post, or it will never appear on\n# the live site.\ndraft: false\n---\n\n<!--\n  NOTE TO FUTURE ME: don't write new posts from a blank file. Copy this\n  whole \"example-post\" folder, rename it (the folder name becomes part\n  of the post's URL, so use something short and readable, e.g.\n  \"phd-year-one-reflections\"), edit index.qmd inside it, and delete the\n  \"draft: true\" line above once it's ready to publish. This comment and\n  the rest of this page are template scaffolding - they won't appear on\n  the live site because of \"draft: true\", and this HTML comment itself\n  never renders at all.\n-->\n\nThis page is a working template, not a real post - it exists to show the correct pattern for writing one. Everything below demonstrates something worth copying.\n\n## Heading Structure\n\nQuarto turns the `title:` field above into the page's main heading automatically, so headings inside the post body should start one level down, at `##` (not `#` - using `#` here would create a second, duplicate top-level heading).\n\n-   `##` (H2) marks a major section of the post, like this one.\n-   `###` (H3) marks a subsection within it, like the one directly below.\n\nQuarto builds the page's table of contents from these headings, so keeping them in order (H2 before H3, no skipped levels) keeps the TOC sensible.\n\n### A Subsection Example\n\nThis is what a subsection looks like - one level below the section it belongs to.\n\n## Code Chunks\n\nCode chunks use three backticks and curly braces naming the language, e.g. ```` ```{r} ```` or ```` ```{python} ````. The `#|` lines at the top of a chunk are **chunk options** - settings for that one chunk only. Two of the most useful:\n\n-   `echo` - whether the code itself is shown on the page (`true`) or hidden, showing only its output (`false`).\n-   `eval` - whether the code actually *runs* when the site is rendered (`true`), or is just displayed as text without being executed (`false`). Useful for showing code that depends on something not guaranteed to be available at render time - a package, an API key, a large dataset - without breaking the build.\n\n### R example\n\nThis site's other R pages (the LinkedIn games and Bayesian analysis projects) both use this same R engine, so this is the more \"native\" language for this site.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmean(c(4, 8, 15, 16, 23, 42))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 18\n```\n\n\n:::\n:::\n\n\n### Python example\n\nPython chunks work the same way, through the same R-based engine (via a package called `reticulate` that lets R call out to Python) - as long as the post's front matter does *not* add a `jupyter: python3` line. Setting `eval: false` here on purpose, just to demonstrate the option; remove it in a real post if you want the code to actually run.\n\n\n::: {.cell}\n\n```{.python .cell-code}\nimport statistics\nstatistics.mean([4, 8, 15, 16, 23, 42])\n```\n:::\n\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}